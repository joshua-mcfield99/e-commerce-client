export default function Login() {
    const handleLogin = () => {
      // Handle login logic here (e.g., using NextAuth.js or Firebase)
    };
  
    return (
      <div>
        <h1>Login</h1>
        <button onClick={handleLogin}>Login</button>
      </div>
    );
}